# Menu images

Keep optimized local assets grouped by category. Product records reference a small semantic image key in `data/menu.ts`; the UI maps that key to a file in `components/ProductCard.tsx`.

Recommended replacement format: WebP, roughly 800×600 for cards and 1600×1000 for the hero, normally under 250 KB per image.
